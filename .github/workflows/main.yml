on:
  issues:
    types: [opened]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Hello world action step
        id: hello
        uses: Aveline-art/javascript-template-test@main
        with:
          myToken: ${{ secrets.GITHUB_TOKEN }}
